server:
  port: 80

eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/
#在feign 开启 hystrix 的服务降级
feign:
  hystrix:
    enabled: true